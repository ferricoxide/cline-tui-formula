# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: cline-tui
  rootgroup: root
  config: '/etc/cline-tui'
  service:
    name: cline-tui
  subcomponent:
    config: '/etc/cline-tui-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
